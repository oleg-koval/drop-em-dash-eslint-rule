# Agents

## Em dash linting

When work involves **forbidding Unicode em dash (—, U+2014)** in JavaScript/TypeScript (or configuring the **`eslint-plugin-drop-em-dash`** package), read:

`.cursor/skills/drop-em-dash-eslint/SKILL.md`

For global Cursor use, copy or symlink that directory to `~/.cursor/skills/drop-em-dash-eslint`.
